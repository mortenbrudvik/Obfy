<ui:FluentWindow x:Class="Obfy.UI.Views.Dialogs.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="About Obfy" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <ui:SymbolIcon Symbol="Shield24" FontSize="48" Margin="0,0,0,16"/>
            <TextBlock Text="Obfy" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Version 1.0.0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                      HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text=".NET Obfuscation Tool" HorizontalAlignment="Center" FontSize="14"/>
            <TextBlock Text="Protect your .NET assemblies with professional obfuscation"
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                      HorizontalAlignment="Center" Margin="0,8,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
        </StackPanel>

        <!-- Close Button -->
        <ui:Button Grid.Row="2" Content="Close" HorizontalAlignment="Center"
                  Click="CloseButton_Click" Width="100"/>
    </Grid>
</ui:FluentWindow>
